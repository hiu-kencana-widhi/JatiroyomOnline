# Frontend Folder

Currently, this project runs on a monolithic Laravel 12 architecture. The main views, styling, and asset compilation are located within the `backend/resources/` directory.

If you wish to build a separate/decoupled frontend (e.g., using Next.js, React, or Vue) in the future, you can place your frontend application code inside this directory.
